<h1>BURGERS!</h1>
<img src="../assets/img/burger.png" alt="Burger Picture" height="145px" width ="200px">

<h2>Ready to Eat your Burger?</h2>

<ul>
{{#each burgers}}
{{#unless devoured}}
<li>{{this.burger_name}}
        <button class="change-devoured" data-id="{{id}}" data-devoured="{{devoured}}">Devour It Now</button>
    </li>
    {{/unless}}
    {{/each}}
</ul>

<h2>Burgers Devoured</h2>
{{#each burgers}}
    {{#if devoured}}
    <li>{{this.burger_name}}
    </li>
      {{/if}}
    {{/each}}
</ul>

<h2>BYOB (Build Your Own Burger):</h2>
<form id="create-form">

    <div class="form-group">
        <label for="ca">What's your burger's name?</label>
        <input type="text" id="ca" name="name">
    </div>

    <button type="submit">Add Your Burger!</button>
</form>
